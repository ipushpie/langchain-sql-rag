# API Keys
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_GEMINI_MODEL_NAME=gemini-1.5-flash

# Database URLs
NODE_DATABASE_URL=postgresql://username:password@localhost:5432/nodedb
DD_DATABASE_URL=postgresql://username:password@localhost:5432/dddb

# Ollama Configuration (fallback if no Google API key)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL_NAME=llama3.1

# Logging Configuration
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG: Most verbose, includes all debugging information
# INFO: General information, good for production  
# WARNING: Only warnings and errors
# ERROR: Only errors and critical issues
# CRITICAL: Only critical failures

# Optional: Log to file (uncomment to enable file logging)
# LOG_FILE=logs/app.log